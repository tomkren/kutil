/**
 * Balíček tříd zajišťující převod z XML reprezentace do KObjectové reprezentace, dále obsahuje
 * třídy pro rerezentaci XML struktury a interní XML soubory programu
 * (klíčové třída: XmlLoader ).
 */
package kutil.xml;
